[alias]
    st = status
    aa = add -A
    df = diff
    ca = commit -a
    caa = commit -a --amend --date=now
    save = commit -a -m "Save"
    br = branch -vv
    co = checkout
    gr = log --all --graph --decorate

[user]
	email = peiyingliu.phys@gmail.com
	name = deeperlearner

[core]
    excludesfile = ~/.gitignore_global

# Allow local customizations in the .gitconfig_local file
[include]
	path = ~/.gitconfig_local
[color]
	ui = auto
